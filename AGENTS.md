# AIエージェント開発ガイド

このファイルは、スマートフォン向けWebアプリ「ぬいと」で作業するAIエージェント向けの共通ガイドです。ぬいとは、ぬいぐるみとの外出を写真・ひとこと・場所・時刻で記録し、地図と日記で振り返る個人向けアプリです。

## プロダクト要件の参照

要件の正本は `docs/requirements/` とします。作業開始時は次の順で確認します。

1. `docs/requirements/README.md`
2. `docs/requirements/01-product-overview.md`
3. `docs/requirements/02-domain-rules.md`
4. 実装対象に対応する詳細文書

要件の優先度は `Must`、`Should`、`Future`、`暫定` の順です。同じ優先度で矛盾する場合は、より新しい文書バージョン、ドメインルール、要件ID付きの記述、画面例・参考例の順に優先します。判断できない矛盾は勝手に解消せず、影響を明示します。

## プロダクト固有の制約

- 投稿本文は最大300文字、1投稿の写真は0〜4枚、一日のまとめ本文は最大1000文字とする。
- 保存画像は縦横比を維持して圧縮し、正方形へ破壊的に切り抜かない。サムネイルの表示上のトリミングは許可する。
- 地図は本人用とし、公開URL、フォロー、いいね、コメント、他ユーザー閲覧を実装しない。
- 足あと線は実際の移動経路ではなく、場所と時刻のある投稿地点を時刻順に結ぶ点線とする。時刻不明投稿は含めない。
- 「きょうの日記」は、時刻あり投稿を時刻昇順で先に並べ、時刻不明投稿を投稿日時昇順で末尾に並べる。
- 一日の切り替え時刻は初期値00:00、日記案内時刻は初期値21:00とし、個別に変更可能とする。案内時刻は締切ではない。
- 初期版ではプッシュ通知を実装しない。主要保存先はブラウザ内ローカルストレージとし、LINEログインとクラウド同期は将来機能とする。
- 投稿日時を行動時刻として代用せず、未確定事項を確定要件として扱わない。
- 仕様変更時は、関連する要件ID、受入基準、トレーサビリティ表を同じ変更単位で更新する。
- 実装判断で要件を補う場合は、`docs/requirements/08-roadmap-and-open-issues.md` に決定事項として記録する。

## 基本方針

- ユーザー要求、既存仕様、既存コード、テストを確認してから変更する。
- 変更範囲を必要最小限にし、無関係な変更を混在させない。
- 既存の命名、構成、設計パターンを尊重する。
- 将来の可能性だけを理由に過剰設計しない。
- 同じ処理や同じ規則を重複して実装・記載しない。
- セキュリティ、プライバシー、ライセンスを考慮する。
- エラーを握りつぶさず、実行していない検証を実行済みと報告しない。
- Secretや本番認証情報をコード、設定、ログへ出力しない。
- 仕様が不明確な場合は、推測で大きな仕様変更を行わない。

## 記述言語

- 利用者への報告、コミットメッセージ、ソースコード内のコメント、プロジェクト内で新しく作成する文書は、プロジェクト固有の規則またはユーザーの指定がない限り日本語で記述する。
- コミットメッセージは、変更の目的が分かる簡潔な日本語を基本とする。
- コードコメントは、処理の逐語説明ではなく、理由、制約、非自明な判断を日本語で記録する。
- 識別子、API名、コマンド、エラーメッセージ、外部仕様の用語、ライセンス原文は、不自然に翻訳しない。
- generated code、vendor code、外部projectへ送る変更、採用技術の標準形式など、別の言語や形式が必要な場合はそちらを優先する。

## 情報の参照順序

原則として次の順で確認します。

1. ユーザー要求、Issue、タスク
2. `AGENTS.md`
3. 関連する `docs/`
4. 関連する `.agents/skills/*/SKILL.md`
5. 既存コードと設定
6. テスト
7. `README.md` などの補助資料

情報が矛盾する場合は勝手に一方を採用せず、矛盾と影響を明示します。

## ディレクトリの責務

- `AGENTS.md`: リポジトリ全体の原則と制約
- `.agents/skills/`: 繰り返し利用する作業手順と判断基準
- `.codex/agents/`: 専門Agentの責務と権限
- `docs/`: 本プロジェクト固有の仕様、設計、運用情報

プロジェクト仕様をSkillやAgentへコピーせず、必要に応じて `docs/` を参照させます。

## Skillの利用

作業内容に対応するSkillがある場合は、作業前に全文を確認します。

| 作業 | Skill |
|---|---|
| 機能実装・バグ修正 | `implementation` |
| 不具合の再現・原因調査 | `debugging` |
| テンプレートからの新規プロジェクト初期化 | `project-bootstrap` |
| テスト設計・実装・実行 | `testing` |
| テストコードレビュー | `test-review` |
| 総合コードレビュー | `code-review` |
| コーディング規約 | `coding-standards` |
| 責務分割・モジュール構造 | `modularity` |
| データベース変更 | `database` |
| HTTP API変更 | `api-development` |
| ドキュメント管理 | `documentation` |
| ローカル開発環境 | `development-environment` |
| CI/CD | `ci-cd` |
| リリース管理 | `release-management` |
| ログ・監視・可観測性 | `observability` |
| セキュリティレビュー | `security-review` |
| 性能・コスト監査 | `performance-audit` |
| アクセシビリティレビュー | `accessibility-review` |
| Skill作成・整理 | `skill-authoring` |
| AI機能 | `ai-feature` |
| 依存関係管理 | `dependency-management` |

複数領域にまたがる変更では必要なSkillだけを組み合わせます。SkillとAgentの権限が衝突する場合は、Agentの権限を超えて作業しません。

## Agentの利用

通常の実装はメインAgentが担当します。独立した調査・レビュー・基盤作業に価値がある場合だけ、`.codex/agents/` の専門Agentを利用します。UIのアクセシビリティ監査には `accessibility_reviewer`、責務分割と保守性の監査には `maintainability_reviewer` を利用できます。単純な変更を過剰に分割しません。

## 作業開始前

1. 目的と完了条件を確認する。
2. 関連するdocs、Skill、既存コード、設定、テストを確認する。
3. 変更範囲と影響範囲を整理する。
4. 既存の仕組みで実現できるか確認する。
5. 依存関係や外部サービスの変更が必要なら、最新の一次情報を確認する。
6. 実装タスクでは `docs/management/delivery-governance.md` のDefinition of Readyを確認する。

## 実装と検証

- 小さな単位で変更する。
- ユーザー入力と外部入力を信頼せず、境界で検証する。
- 破壊的変更、データ移行、互換性への影響を確認する。
- 変更に応じてformatter、lint、typecheck、test、buildを実行する。
- テストを無効化したり期待値を弱めたりして失敗を回避しない。
- 仕様、実装、テスト、docsを同じ変更単位で整合させる。
- 重要な技術判断はADRへ記録し、置き換える場合は過去の判断との関係を残す。
- 行数だけでなく、変更理由、状態の所有、依存方向に基づいてファイルと関数の責務を分ける。

実在するコマンドは本プロジェクトの設定から確認し、存在しないコマンドを想像で実行しません。

## 外部操作

- 実装・修正を依頼された作業では、要求を満たす変更と検証が完了した後のローカルコミットまでを依頼範囲に含むものとし、コミット前の追加確認は不要とする。ユーザーがコミットしないよう指定した場合は従う。
- 本番デプロイや外部システムの更新は、明示的な依頼と対象確認なしに実行しない。
- 本番へのデプロイは、その時点のユーザーメッセージで明示的に指示された場合だけ実行する。過去の包括的な公開依頼、以前のデプロイ許可、修正依頼、会話上の流れを、新たな本番デプロイの許可として引き継がない。
- 「修正する」「確認する」「公開準備をする」など、デプロイを明示しない依頼では、ローカル変更、テスト、build、dry-runまでに留める。本番反映が必要でも自動では実行せず、変更内容と対象環境を示して改めて許可を得る。
- 本番デプロイを含むコマンドを開始する直前に、対象環境がproductionであることと、現在の依頼に本番デプロイの明示指示があることを再確認する。
- `git push` は手動操作とし、AIエージェントは実行しない。
- タグ作成や公開操作は、リポジトリ固有の方針とユーザーの許可を確認する。
- 未pushの場合は、必要な対象と手動コマンドを案内する。

## 作業完了時

- 要求を満たしているか確認する。
- 無関係な変更とSecretが含まれていないか確認する。
- 必要なテストとdocsが存在するか確認する。
- Definition of DoneとPull Requestの確認項目を満たしているか確認する。
- 実行した検証と結果を報告する。
- 実行できなかった検証、未解決事項、残るリスクを明示する。

最優先するのは、本プロジェクトの意図と既存設計を尊重し、小さく安全な変更を積み重ねることです。
